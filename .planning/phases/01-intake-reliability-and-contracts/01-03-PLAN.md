---
phase: 01-intake-reliability-and-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - core/contracts.py
  - core/artifacts.py
  - agents/inbox/agent.py
  - evals/logger.py
  - tests/test_artifact_contracts.py
  - tests/test_evals.py
autonomous: true
requirements:
  - OPS-04
user_setup: []
must_haves:
  truths:
    - Every successful run writes canonical, versioned JSON artifacts for job extraction, resume output, and eval output.
    - Artifact files are stored in deterministic run-scoped paths and linked in run context/logs.
    - Artifact contract validation prevents malformed payloads from being silently persisted.
  artifacts:
    - core/contracts.py defines schema-versioned canonical artifact models.
    - core/artifacts.py writes canonical artifact payloads to deterministic filesystem locations.
    - agents/inbox/agent.py emits extraction/resume/eval artifact payloads per run.
    - tests/test_artifact_contracts.py validates schema and writer behavior.
  key_links:
    - agent pipeline output is serialized through core/contracts.py and persisted via core/artifacts.py.
    - evals/logger.py references canonical artifact paths in run context for replay/debug workflows.
---

<objective>
Establish schema-versioned canonical run artifacts for extraction, resume output, and eval output.

Purpose: Reliable operations and replayability require stable contracts independent of ad hoc run logs.
Output: Canonical artifact schemas, deterministic writer, pipeline integration, and contract tests.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-reliability-and-contracts/01-RESEARCH.md
@.planning/phases/01-intake-reliability-and-contracts/01-VALIDATION.md
@agents/inbox/agent.py
@evals/logger.py
@tests/test_evals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define versioned canonical artifact contracts</name>
  <files>core/contracts.py, tests/test_artifact_contracts.py</files>
  <action>Create strict models for `job_extraction`, `resume_output`, and `eval_output` artifacts with shared fields (`schema_version`, `run_id`, `created_at`, trace metadata). Enforce required fields and deterministic serialization behavior through tests.</action>
  <verify>./.venv/bin/pytest -q tests/test_artifact_contracts.py</verify>
  <done>Contract module validates required fields and fails invalid payloads with clear errors; schema version is explicitly asserted in tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement artifact writer and deterministic path policy</name>
  <files>core/artifacts.py, tests/test_artifact_contracts.py</files>
  <action>Implement artifact persistence helper that writes canonical JSON files under `runs/artifacts/<run_id>/` with stable filenames and atomic write semantics. Ensure writer returns path references used by downstream logging.</action>
  <verify>./.venv/bin/pytest -q tests/test_artifact_contracts.py tests/test_evals.py</verify>
  <done>Artifact writer persists all canonical files to deterministic locations and returns paths consumed by pipeline/logging.</done>
</task>

<task type="auto">
  <name>Task 3: Wire canonical artifacts into inbox pipeline and run logs</name>
  <files>agents/inbox/agent.py, evals/logger.py, tests/test_evals.py</files>
  <action>Integrate contract serialization + writer calls into pipeline completion flow so each run emits extraction/resume/eval artifacts and stores references in run context. Keep existing `run-*.json` behavior backward compatible while adding canonical path linkage.</action>
  <verify>./.venv/bin/pytest -q tests/test_evals.py tests/test_resume.py tests/test_jd.py</verify>
  <done>Completed runs include canonical artifact files and context references without regressing existing run logging behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_artifact_contracts.py tests/test_evals.py
- [ ] ./.venv/bin/pytest -q tests/test_resume.py tests/test_jd.py
- [ ] ./.venv/bin/pytest -q tests/test_webhook_api_e2e.py tests/test_router.py tests/test_url_ingest.py
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical versioned JSON artifacts exist for extraction, resume output, and eval output on completed runs

</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-reliability-and-contracts/01-03-SUMMARY.md`
</output>
