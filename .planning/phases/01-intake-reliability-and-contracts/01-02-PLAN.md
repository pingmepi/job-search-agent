---
phase: 01-intake-reliability-and-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/router.py
  - agents/inbox/adapter.py
  - agents/inbox/url_ingest.py
  - tests/test_router.py
  - tests/test_router_article_handling.py
  - tests/test_url_ingest.py
  - tests/test_integration_pipeline_adapter.py
autonomous: true
requirements:
  - ING-01
  - ING-02
  - OPS-03
user_setup: []
must_haves:
  truths:
    - Telegram intake accepts URL, image, and text paths that deterministically route to pipeline or safe fallback branches.
    - URL extraction failures always return a clear screenshot fallback prompt and stop unsafe continuation.
    - Ambiguous/non-job and article-style content are deterministically handled with explicit route targets and reason codes.
  artifacts:
    - core/router.py includes deterministic ARTICLE and AMBIGUOUS_NON_JOB-style routing outcomes with machine-stable reason codes.
    - agents/inbox/adapter.py handles all route branches with clear user-facing responses.
    - tests/test_router_article_handling.py covers article/non-job routing branches.
    - tests/test_url_ingest.py and tests/test_integration_pipeline_adapter.py cover URL fallback contract.
  key_links:
    - adapter text handler consumes router reason codes and route targets for telemetry.
    - URL ingest error typing drives screenshot fallback behavior in adapter.
---

<objective>
Stabilize intake contract so all Telegram job-submission modes are reliable and non-job inputs follow deterministic branches.

Purpose: Reliable user intake requires strict routing behavior and explicit fallback UX for URL extraction failures.
Output: Expanded deterministic router, adapter branch handling, and contract tests for URL fallback and non-job routing.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-reliability-and-contracts/01-RESEARCH.md
@.planning/phases/01-intake-reliability-and-contracts/01-VALIDATION.md
@core/router.py
@agents/inbox/adapter.py
@agents/inbox/url_ingest.py
@tests/test_router.py
@tests/test_url_ingest.py
@tests/test_integration_pipeline_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand deterministic router outcomes for article and ambiguous inputs</name>
  <files>core/router.py, tests/test_router.py, tests/test_router_article_handling.py</files>
  <action>Add explicit non-job branches (including article-style long-form detection) with stable reason-code outputs for telemetry and assertions. Keep deterministic, pattern-based routing and avoid LLM-driven decisions.</action>
  <verify>./.venv/bin/pytest -q tests/test_router.py tests/test_router_article_handling.py</verify>
  <done>Router returns explicit target + reason code for JD, article, ambiguous non-job, profile, and follow-up categories; branch tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement adapter handling for new route branches and telemetry fields</name>
  <files>agents/inbox/adapter.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Update adapter route handling to support new deterministic branch IDs and route-specific user messages. Add structured logging fields (`route_target`, `route_reason_code`, `input_mode`, `update_id`) so operations can diagnose intake outcomes without parsing prose logs.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py tests/test_router.py</verify>
  <done>Adapter handles all router targets without fall-through and emits structured route telemetry in logs.</done>
</task>

<task type="auto">
  <name>Task 3: Codify URL failure screenshot fallback as a contract</name>
  <files>agents/inbox/url_ingest.py, agents/inbox/adapter.py, tests/test_url_ingest.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Classify URL ingest failure types and centralize fallback prompt text constant in adapter. Ensure failed URL extraction exits before pipeline run and always prompts screenshot fallback with consistent wording.</action>
  <verify>./.venv/bin/pytest -q tests/test_url_ingest.py tests/test_integration_pipeline_adapter.py</verify>
  <done>URL failure path is deterministic, tested, and uses one canonical fallback prompt with no silent continuation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_router.py tests/test_router_article_handling.py
- [ ] ./.venv/bin/pytest -q tests/test_url_ingest.py tests/test_integration_pipeline_adapter.py
- [ ] ./.venv/bin/pytest -q tests/test_webhook_api_e2e.py
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Intake behavior is deterministic for job and non-job inputs, with URL screenshot fallback guaranteed

</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-reliability-and-contracts/01-02-SUMMARY.md`
</output>
