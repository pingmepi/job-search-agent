---
phase: 01-intake-reliability-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - main.py
  - core/db.py
  - tests/test_webhook_events.py
  - tests/test_replay_webhook_cli.py
autonomous: true
requirements:
  - ING-03
user_setup: []
must_haves:
  truths:
    - Raw Telegram webhook payloads are persisted before update processing with stable identifiers and timestamps.
    - Webhook events can be replayed by event ID or update ID without manual payload reconstruction.
    - Event status transitions are visible for success/failure and retry outcomes.
  artifacts:
    - core/db.py contains webhook_events schema and CRUD helpers.
    - main.py exposes replay command for persisted webhook events.
    - tests/test_webhook_events.py validates persistence and status lifecycle.
    - tests/test_replay_webhook_cli.py validates replay entrypoints.
  key_links:
    - app.py writes event records through core/db.py before process_update.
    - main.py replay command reuses the same update-processing path as webhook ingestion.
---

<objective>
Harden webhook intake so each inbound Telegram event is durably recorded and replayable.

Purpose: Reliable ingestion requires deterministic postmortem and replay capability beyond in-memory dedupe.
Output: Durable webhook event store, replay command, and focused tests.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intake-reliability-and-contracts/01-RESEARCH.md
@.planning/phases/01-intake-reliability-and-contracts/01-VALIDATION.md
@app.py
@main.py
@core/db.py
@tests/test_webhook_api_e2e.py
@tests/test_webhook_retries.py
@tests/test_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable webhook event persistence schema and helpers</name>
  <files>core/db.py, tests/test_webhook_events.py</files>
  <action>Add a `webhook_events` table with event IDs, update IDs, timestamps, headers/payload JSON, secret validation flags, processing status, route linkage, and error fields. Implement insert/get/list/update helpers with idempotent writes and migration-safe initialization. Add unit/integration tests that assert create/read/update behavior and stable timestamp population.</action>
  <verify>./.venv/bin/pytest -q tests/test_webhook_events.py tests/test_db.py</verify>
  <done>`webhook_events` exists after `init-db`; helpers support persist + state transition + lookup by event/update ID; new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Persist webhook envelope and lifecycle transitions in FastAPI handler</name>
  <files>app.py, tests/test_webhook_api_e2e.py, tests/test_webhook_retries.py</files>
  <action>Instrument the webhook endpoint to persist inbound envelope data immediately after secret validation and before `process_update`. Track status across attempts (`received`, `processing`, `processed`, `failed`) with error text and optional run linkage. Preserve existing at-most-once behavior while ensuring records survive process restart.</action>
  <verify>./.venv/bin/pytest -q tests/test_webhook_api_e2e.py tests/test_webhook_retries.py tests/test_webhook_events.py</verify>
  <done>Webhook requests create durable event records, retries update the same event row, and existing webhook retry/dedupe tests remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Add replay CLI command for persisted events</name>
  <files>main.py, tests/test_replay_webhook_cli.py</files>
  <action>Add `replay-webhook` CLI command that accepts `--event-id` or `--update-id`, retrieves the persisted payload, and re-submits it through the same processing path with safe defaults for side-effectful integrations. Add CLI tests for argument validation and successful replay invocation.</action>
  <verify>./.venv/bin/pytest -q tests/test_replay_webhook_cli.py tests/test_webhook_events.py</verify>
  <done>Replay command is discoverable in `main.py` usage, rejects invalid argument combinations, and can replay a stored event deterministically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/python main.py init-db
- [ ] ./.venv/bin/pytest -q tests/test_webhook_events.py tests/test_replay_webhook_cli.py tests/test_webhook_api_e2e.py tests/test_webhook_retries.py
- [ ] ./.venv/bin/pytest -q tests/test_db.py
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Webhook events are durably persisted and replayable by stable identifiers

</success_criteria>

<output>
After completion, create `.planning/phases/01-intake-reliability-and-contracts/01-01-SUMMARY.md`
</output>
