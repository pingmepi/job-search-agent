---
phase: 03-collateral-selection-and-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - integrations/drive.py
  - agents/inbox/agent.py
  - tests/test_integration_pipeline_adapter.py
autonomous: true
requirements:
  - COL-03
user_setup: []
must_haves:
  truths:
    - Resume PDF and selected collateral files are uploaded to Google Drive under a per-application folder structure keyed by canonical `application_context_id`.
    - Upload payload includes only generated collateral types for the application, not all possible collateral types.
    - Upload outcomes (links/status/failures) are persisted in run metadata without making local artifact generation fail-hard.
  artifacts:
    - integrations/drive.py provides per-application folder creation and multi-file upload behavior.
    - agents/inbox/agent.py builds upload file list from local produced-file manifest and persists upload results.
    - tests/test_integration_pipeline_adapter.py verifies upload payload composition and returned drive metadata persistence.
  key_links:
    - produced local artifact manifest from Plan 02 is the single source for drive upload file selection.
    - drive upload return values are serialized into run outputs that align with collateral selection and local artifact state.
---

<objective>
Deliver per-application Google Drive uploads for resume and selected collateral artifacts.

Purpose: COL-03 requires organized Drive delivery of generated application artifacts with per-application folder structure.
Output: Drive batch upload/folder API enhancements, pipeline upload wiring from produced manifest, and integration tests for payload and metadata semantics.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collateral-selection-and-delivery/03-RESEARCH.md
@.planning/phases/03-collateral-selection-and-delivery/03-VALIDATION.md
@.planning/phases/03-collateral-selection-and-delivery/03-01-PLAN.md
@.planning/phases/03-collateral-selection-and-delivery/03-02-PLAN.md
@integrations/drive.py
@agents/inbox/agent.py
@tests/test_integration_pipeline_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Drive integration to upload per-application artifact sets</name>
  <files>integrations/drive.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Implement/extend Drive helper(s) to create or resolve per-application folders (e.g., `Jobs/{Company}/{Role}/{application_context_id}`) and upload multiple files (resume + selected collateral). Return structured per-file status/link metadata instead of single URL output.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "drive and upload"</verify>
  <done>Drive integration can upload a selected artifact set into a deterministic per-application folder and returns parseable result metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline upload input from produced local artifact manifest</name>
  <files>agents/inbox/agent.py, integrations/drive.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Build drive upload file lists exclusively from produced local outputs (PDF + selected collateral), preserve `skip_upload` semantics, and keep upload failures non-fatal while recording failure details in metadata.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "drive and (skip_upload or collateral or artifact)"</verify>
  <done>Pipeline sends only produced artifacts to Drive and preserves run completion with explicit metadata when partial upload failures occur.</done>
</task>

<task type="auto">
  <name>Task 3: Persist and assert Drive linkage in integration outputs</name>
  <files>agents/inbox/agent.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Persist Drive folder/file links (or status) into run output/eval metadata fields that downstream workflows can consume. Add integration assertions for selected-only uploads, folder organization shape, and metadata presence.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "drive and metadata"</verify>
  <done>Integration tests confirm Drive upload metadata reflects actual selected local artifacts and per-application folder organization.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "drive or collateral"
- [ ] ./.venv/bin/pytest -q tests/test_artifact_contracts.py
- [ ] ./.venv/bin/pytest -q
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Selected resume + collateral artifacts are uploaded to per-application Drive folders with persisted status/links per COL-03

</success_criteria>

<output>
After completion, create `.planning/phases/03-collateral-selection-and-delivery/03-03-SUMMARY.md`
</output>
