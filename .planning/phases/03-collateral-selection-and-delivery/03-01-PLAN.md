---
phase: 03-collateral-selection-and-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/inbox/adapter.py
  - agents/inbox/agent.py
  - agents/inbox/drafts.py
  - tests/test_integration_pipeline_adapter.py
autonomous: true
requirements:
  - COL-01
user_setup: []
must_haves:
  truths:
    - Telegram intake asks the user which collateral types to generate before draft generation runs.
    - Pipeline generates only the selected collateral types and does not generate unselected types by default.
    - Selection and generation outcomes are persisted as explicit metadata for run traceability.
  artifacts:
    - agents/inbox/adapter.py captures and normalizes collateral selections from user input.
    - agents/inbox/agent.py accepts selected collateral and gates draft generation paths accordingly.
    - tests/test_integration_pipeline_adapter.py includes selective-generation assertions for selected vs unselected collateral.
  key_links:
    - adapter-normalized selection is passed into run_pipeline and consumed by generation gating logic.
    - generated collateral metadata in pipeline output matches files actually produced.
---

<objective>
Implement user-driven collateral selection so only requested outreach drafts are generated.

Purpose: COL-01 requires explicit user choice and selective collateral generation instead of generate-all behavior.
Output: Adapter prompt/normalization flow, pipeline selection plumbing, selective draft-generation logic, and regression tests.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collateral-selection-and-delivery/03-RESEARCH.md
@.planning/phases/03-collateral-selection-and-delivery/03-VALIDATION.md
@agents/inbox/adapter.py
@agents/inbox/agent.py
@agents/inbox/drafts.py
@tests/test_integration_pipeline_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic collateral-selection capture in Telegram adapter</name>
  <files>agents/inbox/adapter.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Add an intake step that asks which collateral types to generate (`email`, `linkedin`, `referral`), normalize free-form choices into a deterministic set, and handle ambiguous responses with explicit re-prompt until valid selection or abort collateral generation for that run (no default generation). Keep routing behavior for non-job paths unchanged.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "collateral and adapter"</verify>
  <done>Adapter emits normalized selected-collateral values for INBOX runs, and tests cover valid choices, mixed-case inputs, ambiguity re-prompt, and no-default behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Thread selected collateral through pipeline entrypoint</name>
  <files>agents/inbox/adapter.py, agents/inbox/agent.py</files>
  <action>Extend pipeline invocation and `run_pipeline` signature to require explicit `selected_collateral` for collateral generation paths. If selection is missing/invalid, abort collateral generation for that run and persist the reason instead of using any default generation behavior.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "pipeline and collateral"</verify>
  <done>Pipeline enforces explicit selection semantics, records missing/invalid-selection outcomes, and exposes selection metadata for downstream tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Gate draft generation and persist generation outcomes</name>
  <files>agents/inbox/agent.py, agents/inbox/drafts.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Gate each collateral generator call by normalized selection, persist selected/generated collateral fields in run context or artifact metadata, and ensure non-selected collateral remains absent (`None`/missing file) rather than generated then discarded.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "collateral and (selected or generated)"</verify>
  <done>Only requested collateral is generated and persisted, and integration tests assert unselected collateral is not produced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "collateral"
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "pipeline"
- [ ] ./.venv/bin/pytest -q tests/test_evals.py
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Collateral selection is explicit and only selected collateral types are generated per COL-01

</success_criteria>

<output>
After completion, create `.planning/phases/03-collateral-selection-and-delivery/03-01-SUMMARY.md`
</output>
