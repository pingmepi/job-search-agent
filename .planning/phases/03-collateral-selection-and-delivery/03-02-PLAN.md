---
phase: 03-collateral-selection-and-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - agents/inbox/agent.py
  - core/contracts.py
  - core/artifacts.py
  - tests/test_artifact_contracts.py
  - tests/test_integration_pipeline_adapter.py
autonomous: true
requirements:
  - COL-02
user_setup: []
must_haves:
  truths:
    - Resume and selected collateral are stored in one dedicated local folder per application context.
    - A single canonical `application_context_id` (deterministic company+role+JD key) is used for foldering and linkage.
    - Canonical artifacts reference concrete produced-file paths for resume and collateral outputs.
    - Local output structure is deterministic and replay-friendly for the same application context keying strategy.
  artifacts:
    - core/contracts.py includes application output directory and collateral file mapping fields.
    - agents/inbox/agent.py writes resume and selected collateral into the same application output directory.
    - tests/test_artifact_contracts.py and tests/test_integration_pipeline_adapter.py validate folder layout and artifact linkage.
  key_links:
    - `application_context_id` generated in pipeline runtime is used as the folder key and maps directly into contract fields serialized by artifact writers.
    - integration assertions confirm filesystem outputs and artifact metadata stay in sync.
---

<objective>
Unify local output storage so each application has one dedicated folder linking resume and collateral artifacts.

Purpose: COL-02 requires generated collateral to be stored per application and linked to that application context.
Output: Contract extensions, unified local output folder behavior, produced-files manifest plumbing, and artifact/integration tests.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collateral-selection-and-delivery/03-RESEARCH.md
@.planning/phases/03-collateral-selection-and-delivery/03-VALIDATION.md
@.planning/phases/03-collateral-selection-and-delivery/03-01-PLAN.md
@agents/inbox/agent.py
@core/contracts.py
@core/artifacts.py
@tests/test_artifact_contracts.py
@tests/test_integration_pipeline_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend artifact contracts for application-folder linkage</name>
  <files>core/contracts.py, tests/test_artifact_contracts.py</files>
  <action>Add explicit fields for `application_output_dir` and `collateral_files` mapping (email/linkedin/referral) in the resume/application artifact contract. Keep non-selected collateral representable as null/absent with strict validation for known keys.</action>
  <verify>./.venv/bin/pytest -q tests/test_artifact_contracts.py</verify>
  <done>Contract tests assert the new local-folder and collateral-linkage fields serialize and validate correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate local artifact writes into a single application directory</name>
  <files>agents/inbox/agent.py, core/artifacts.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Refactor file write paths so PDF and selected collateral text outputs land in one canonical per-application directory keyed by a deterministic `application_context_id` (not `run_id`). Add/adjust artifact helper(s) only where needed to keep writes atomic and path handling consistent.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "artifact or folder or collateral"</verify>
  <done>Integration tests verify resume and selected collateral are colocated under one application folder and unselected collateral files are not present.</done>
</task>

<task type="auto">
  <name>Task 3: Persist produced-file manifest into canonical artifacts</name>
  <files>agents/inbox/agent.py, core/contracts.py, tests/test_artifact_contracts.py, tests/test_integration_pipeline_adapter.py</files>
  <action>Populate artifact metadata with concrete produced-file paths (resume + selected collateral) and ensure serialization occurs in existing canonical artifact write flow. Avoid introducing parallel metadata sources that can drift.</action>
  <verify>./.venv/bin/pytest -q tests/test_artifact_contracts.py tests/test_integration_pipeline_adapter.py -k "artifact and collateral"
</verify>
  <done>Artifact JSON contains application output directory and collateral path mapping that matches actual filesystem outputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_artifact_contracts.py tests/test_integration_pipeline_adapter.py -k "artifact or collateral"
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "pipeline"
- [ ] ./.venv/bin/pytest -q tests/test_evals.py
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Local resume + collateral outputs are stored per application and linked via canonical artifacts per COL-02

</success_criteria>

<output>
After completion, create `.planning/phases/03-collateral-selection-and-delivery/03-02-SUMMARY.md`
</output>
