---
phase: 02-resume-tailoring-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/hard.py
  - agents/inbox/agent.py
  - core/prompts/resume_mutate_v1.txt
  - core/prompts/resume_condense_v1.txt
  - tests/test_evals.py
  - tests/test_resume.py
autonomous: true
requirements:
  - RES-02
user_setup: []
must_haves:
  truths:
    - Resume mutations remain constrained to editable regions in all mutation paths.
    - Truthfulness checks detect fabricated numeric metrics and newly introduced unsupported entity claims.
    - Pipeline enforces safe behavior when truthfulness checks fail instead of shipping risky mutated output.
  artifacts:
    - evals/hard.py contains stricter truthfulness checks for numeric and entity claim deltas.
    - core/prompts/resume_mutate_v1.txt and core/prompts/resume_condense_v1.txt reinforce grounding constraints.
    - agents/inbox/agent.py records truthfulness counters and applies safe fallback logic when thresholds are exceeded.
    - tests/test_evals.py and tests/test_resume.py cover new truthfulness and edit-scope protections.
  key_links:
    - Prompt grounding rules correspond to evals/hard.py enforcement checks.
    - agents/inbox/agent.py consumes truthfulness check output to decide safe resume mutation/fallback behavior.
---

<objective>
Harden grounded resume mutation controls and enforce truthful-claim safety.

Purpose: RES-02 requires edits to stay inside editable sections while preserving factual claims sourced from existing profile/bullet context.
Output: Stronger truthfulness detectors, mutation prompt constraints, pipeline fail-safe policy, and expanded tests.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resume-tailoring-safety/02-RESEARCH.md
@.planning/phases/02-resume-tailoring-safety/02-VALIDATION.md
@agents/inbox/agent.py
@evals/hard.py
@core/prompts/resume_mutate_v1.txt
@core/prompts/resume_condense_v1.txt
@tests/test_evals.py
@tests/test_resume.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict fabricated-claim detectors in hard evals</name>
  <files>evals/hard.py, tests/test_evals.py</files>
  <action>Implement hard checks for numeric claim regression (new percentages/absolute impact values not grounded in source corpus) and unsupported entity introduction (new org/team/proper-name tokens absent from allowed source text). Keep heuristics deterministic and tune thresholds conservatively to reduce false negatives.</action>
  <verify>./.venv/bin/pytest -q tests/test_evals.py -k "forbidden or truth or numeric or entity"</verify>
  <done>Hard eval layer flags invented metrics/entities with clear error reasons and test fixtures cover pass/fail edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten mutation/condense prompt grounding contract</name>
  <files>core/prompts/resume_mutate_v1.txt, core/prompts/resume_condense_v1.txt, tests/test_resume.py</files>
  <action>Update resume prompts to require grounded rewrites only from provided source material, explicitly prohibiting new claims/metrics/entities, and require a traceable source tag for each mutation rationale where prompt format supports it. Preserve existing structured output expectations used by parser code.</action>
  <verify>./.venv/bin/pytest -q tests/test_resume.py -k "editable or mutate or condense"</verify>
  <done>Prompt text codifies grounding requirements without breaking parser assumptions, and prompt-dependent resume tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce safe pipeline response on truthfulness violations</name>
  <files>agents/inbox/agent.py, tests/test_evals.py, tests/test_resume.py</files>
  <action>Integrate new truthfulness counters into resume pipeline decisions: if violations exceed policy threshold, bypass risky mutation output and use safe content path while still producing diagnostics in eval metadata. Ensure non-editable region guarantees remain intact in this path.</action>
  <verify>./.venv/bin/pytest -q tests/test_evals.py tests/test_resume.py</verify>
  <done>Pipeline never returns mutation output that fails truthfulness policy, and tests assert safe fallback behavior with violation metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_evals.py tests/test_resume.py
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "resume"
- [ ] ./.venv/bin/pytest -q
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Resume mutations remain editable-only and grounded with enforceable truthfulness safeguards per RES-02

</success_criteria>

<output>
After completion, create `.planning/phases/02-resume-tailoring-safety/02-02-SUMMARY.md`
</output>
