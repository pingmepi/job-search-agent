---
phase: 02-resume-tailoring-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - agents/inbox/agent.py
  - agents/inbox/resume.py
  - core/contracts.py
  - tests/test_integration_pipeline_adapter.py
  - tests/test_artifact_contracts.py
  - tests/test_resume.py
autonomous: true
requirements:
  - RES-03
  - RES-04
user_setup: []
must_haves:
  truths:
    - Resume pipeline enforces bounded one-page condense retries and records explicit one-page terminal status.
    - If single-page target is not met or mutated compile fails, safe fallback behavior is deterministic and documented in artifact fields.
    - Resume output artifact always records compile outcome (`mutated_success` or `fallback_success`) and associated diagnostics.
  artifacts:
    - core/contracts.py contains one-page and compile-outcome fields in resume output contract.
    - agents/inbox/agent.py implements explicit condense loop terminal states and compile fallback outcome reporting.
    - tests/test_integration_pipeline_adapter.py validates fallback transitions and compile outcome branches that always end with a valid PDF.
    - tests/test_artifact_contracts.py and tests/test_resume.py assert artifact-field semantics and retry boundaries.
  key_links:
    - agents/inbox/agent.py writes `single_page_status` and `compile_outcome` through core/contracts.py artifact model.
    - Integration tests assert runtime behavior matches declared artifact status values for mutated and fallback success branches.
---

<objective>
Implement explicit single-page terminal behavior and robust compile fallback outcome contracts.

Purpose: RES-03 and RES-04 require bounded one-page enforcement and reliable compilable PDF fallback semantics with transparent outcome reporting.
Output: Pipeline terminal-state refactor, expanded artifact schema fields, and integration coverage for mutated/fallback success paths.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resume-tailoring-safety/02-RESEARCH.md
@.planning/phases/02-resume-tailoring-safety/02-VALIDATION.md
@.planning/phases/02-resume-tailoring-safety/02-01-PLAN.md
@.planning/phases/02-resume-tailoring-safety/02-02-PLAN.md
@agents/inbox/agent.py
@agents/inbox/resume.py
@core/contracts.py
@tests/test_integration_pipeline_adapter.py
@tests/test_artifact_contracts.py
@tests/test_resume.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend artifact contract with one-page and compile outcome states</name>
  <files>core/contracts.py, tests/test_artifact_contracts.py</files>
  <action>Add fields for `single_page_target_met`, `single_page_status`, and `compile_outcome` in resume artifact model. Restrict compile outcome statuses to successful terminal states only (`mutated_success`, `fallback_success`) so downstream code cannot emit non-PDF outcomes.</action>
  <verify>./.venv/bin/pytest -q tests/test_artifact_contracts.py</verify>
  <done>Contract tests enforce allowed one-page/compile statuses and serialize fields consistently in resume output artifact payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor condense loop into explicit one-page terminal branches</name>
  <files>agents/inbox/agent.py, agents/inbox/resume.py, tests/test_resume.py</files>
  <action>Keep bounded condense retries but add explicit post-loop terminal decisioning: one-page achieved, not achieved but tight-layout attempt used, or not achieved and fallback-to-base triggered. Persist terminal status regardless of compile result and keep retry bounds deterministic.</action>
  <verify>./.venv/bin/pytest -q tests/test_resume.py -k "single_page or condense or fallback"</verify>
  <done>Pipeline reports a deterministic single-page terminal status for every resume run and tests verify retry-limit behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Guarantee compile fallback outcome reporting across all branches</name>
  <files>agents/inbox/agent.py, tests/test_integration_pipeline_adapter.py, tests/test_artifact_contracts.py</files>
  <action>Finalize compile path to always attempt safe fallback compile when mutated compile fails or single-page policy requires rollback, and only mark plan complete when one of these branches yields a valid PDF. Populate compile outcome and diagnostics for mutated success and fallback success branches.</action>
  <verify>./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py tests/test_artifact_contracts.py</verify>
  <done>Integration tests cover compile outcome branches and artifact payload remains present with successful PDF semantics for all terminal states.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py tests/test_resume.py tests/test_artifact_contracts.py
- [ ] ./.venv/bin/pytest -q tests/test_evals.py
- [ ] ./.venv/bin/python -m evals.ci_gate
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- One-page enforcement and compile fallback behavior are explicit, bounded, artifact-auditable, and always return a compilable PDF per RES-03 and RES-04

</success_criteria>

<output>
After completion, create `.planning/phases/02-resume-tailoring-safety/02-03-SUMMARY.md`
</output>
