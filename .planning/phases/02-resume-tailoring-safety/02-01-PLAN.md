---
phase: 02-resume-tailoring-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/contracts.py
  - agents/inbox/resume.py
  - agents/inbox/agent.py
  - tests/test_resume.py
  - tests/test_artifact_contracts.py
autonomous: true
requirements:
  - RES-01
user_setup: []
must_haves:
  truths:
    - Resume base selection is deterministic for identical inputs and ties are resolved consistently.
    - Fit score metadata includes enough detail to explain why a base resume was selected.
    - Resume output artifact includes selection metadata needed for run-level traceability.
  artifacts:
    - core/contracts.py includes resume fit score detail fields in resume output artifact schema.
    - agents/inbox/resume.py computes deterministic selection details and tie-break behavior.
    - agents/inbox/agent.py persists selection details into run artifact/eval metadata.
    - tests/test_resume.py and tests/test_artifact_contracts.py assert deterministic selection and artifact field presence.
  key_links:
    - agents/inbox/resume.py selection output maps directly into core/contracts.py resume artifact fields.
    - agents/inbox/agent.py serializes selection metadata through canonical artifact writing.
---

<objective>
Implement deterministic resume-base selection observability and artifact contracts.

Purpose: RES-01 requires selecting the closest resume base and recording fit score metadata that is auditable per run.
Output: Extended resume artifact schema, deterministic selection detail plumbing, and test coverage for metadata completeness.
</objective>

<execution_context>
@/Users/karan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/karan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resume-tailoring-safety/02-RESEARCH.md
@.planning/phases/02-resume-tailoring-safety/02-VALIDATION.md
@core/contracts.py
@agents/inbox/resume.py
@agents/inbox/agent.py
@tests/test_resume.py
@tests/test_artifact_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend resume output contract for fit-score provenance</name>
  <files>core/contracts.py, tests/test_artifact_contracts.py</files>
  <action>Add a structured `fit_score_details` payload on the resume artifact model containing selected resume identifier, candidate count, matched skills, missing skills, tie-break reason, and normalized score. Keep schema evolution explicit and update contract tests to validate required/optional field behavior.</action>
  <verify>./.venv/bin/pytest -q tests/test_artifact_contracts.py</verify>
  <done>Resume artifact schema accepts and validates fit score details, and contract tests assert field presence and serialization behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Make resume selection details deterministic and explainable</name>
  <files>agents/inbox/resume.py, tests/test_resume.py</files>
  <action>Refactor selection helper(s) to return stable detail payloads for every candidate and enforce deterministic tie-breaking using stable ordering. Keep existing overlap strategy, but expose matched/missing skill sets and normalized score in returned metadata used by pipeline callers.</action>
  <verify>./.venv/bin/pytest -q tests/test_resume.py -k "select or fit or tie"</verify>
  <done>Given identical job text and base resumes, selector output (chosen file + details) is deterministic and tested, including tie behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Persist selection metadata through pipeline artifact writes</name>
  <files>agents/inbox/agent.py, tests/test_resume.py, tests/test_artifact_contracts.py</files>
  <action>Wire new selection details into pipeline resume output metadata and any eval context payloads so run artifacts include auditable fit provenance. Ensure existing callers still operate when details are absent (backward compatibility for older runs/tests).</action>
  <verify>./.venv/bin/pytest -q tests/test_resume.py tests/test_artifact_contracts.py</verify>
  <done>Resume output artifact includes fit score details for tailored runs, and regression tests remain green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./.venv/bin/pytest -q tests/test_resume.py tests/test_artifact_contracts.py
- [ ] ./.venv/bin/pytest -q tests/test_integration_pipeline_adapter.py -k "resume or artifact"
- [ ] ./.venv/bin/pytest -q tests/test_evals.py
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Resume base selection and fit score metadata are deterministic and persisted per RES-01

</success_criteria>

<output>
After completion, create `.planning/phases/02-resume-tailoring-safety/02-01-SUMMARY.md`
</output>
